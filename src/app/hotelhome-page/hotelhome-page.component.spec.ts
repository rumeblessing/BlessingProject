import { HotelhomePageComponent } from './hotelhome-page.component';

describe('HotelhomePageComponent', () => {
  let component: HotelhomePageComponent;

  beforeEach(() => {
    component = new HotelhomePageComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
